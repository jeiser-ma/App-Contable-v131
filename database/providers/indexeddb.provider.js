/**
 * IndexedDBProvider — implementación sobre IndexedDB.
 * HU02: stub (mismo contrato que LocalStorageProvider).
 * Implementación: HU14.
 *
 * Contrato (async):
 *   get(key) -> Promise<any|null>
 *   save(key, data) -> Promise<void>
 *   remove(key) -> Promise<void>
 *   clear(keys?) -> Promise<void>
 *   exists(key) -> Promise<boolean>
 */
